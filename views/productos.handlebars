<div class="col-md-6 mx-auto text-center mt-5">
  <div class="header-title">
    <h2 class="wv-heading--subtitle">
      Mis productos
    </h2>
  </div>
</div>

<div class="row mt-4">
  <div class="col-md-6 mx-auto">
    <table class="table table-striped table-condensed">
      <thead>
        <tr>
          <th>#</th>
          <th>Nombre</th>
          <th>Precio</th>
          <th>Imagen</th>
        </tr>
      </thead>
      <tbody>
        {{#each productos}}
        <tr>
          <td>{{this.id}}</td>
          <td>{{this.title}}</td>
          <td>{{this.price}}</td>
          <td> <img src="/assets/img/{{this.thumbnail}}.png" alt=" {{this.thumbnail}}" /></td>
        </tr>
        {{else}}
        <tr>
          <td class="text-center" colspan="4">No hay productos</td>
        </tr>
        {{/each}}
      </tbody>
    </table>
    <div class="login-or">
      <hr class="hr-or">
    </div>
    <p class="small mt-3">Para volver al formulario y seguir agregando productos hacer <a href="/"
        class="ps-hero__content__link">click aquí</a>
    </p>
  </div>
</div>